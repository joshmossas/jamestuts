/// Puzzle 1
///
/// Welcome to Volume 2! Let's start grouping related data together using Maps.
/// Maps use "Keys" (like labels) and "Values" (the actual data).
///
/// 1. Create a Map called `playerProfile`.
/// 2. Give it the following keys and values:
///    - "username" (String) -> "DragonSlayer99"
///    - "level" (int) -> 42
///    - "isOnline" (bool) -> true
/// 3. Print a welcome message by accessing the "username" from the Map:
///    "Welcome back, DragonSlayer99!" (Hint: use `playerProfile["username"]`)

void main() {
  // Your code here
}
