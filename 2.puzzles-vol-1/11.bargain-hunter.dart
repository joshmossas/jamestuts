/// Puzzle 11
///
/// Let's go bargain hunting! You have a list of prices, but you only 
/// want to look at the items that are on clearance (under $20.00).
///
/// 1. Create an empty list called `clearanceItems`. 
///    (Hint: `var clearanceItems = <double>[];` creates an empty list of numbers)
/// 2. Use a `for-in` loop to check every price in `allPrices`.
/// 3. If the price is LESS THAN 20.00, `add()` it to your `clearanceItems` list.
/// 4. When the loop is finished, print your new list.
///
/// Expected output: [15.99, 5.0, 19.99, 2.5]

void main() {
  var allPrices = [45.00, 15.99, 89.99, 5.00, 19.99, 120.00, 2.50];

  // Your code here
}
