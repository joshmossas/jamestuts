/// Puzzle 12
///
/// The store is having a massive sale! Everything is getting a $5 discount.
/// Instead of changing the original prices, we want to create a brand new
/// list with the discounted prices.
///
/// 1. Write a function called `applyDiscount` that takes a `List<double>` as input.
/// 2. The function should return a NEW `List<double>`.
/// 3. Inside the function, create an empty list to hold the new prices.
/// 4. Loop through the input list. For each price, subtract 5.00 and `add()`
///    the result to your new list.
/// 5. Return the new list.
/// 6. In `main()`, call your function with `originalPrices` and print the result.
///
/// Expected output: [15.0, 45.0, 95.0, 5.0]

void main() {
  var originalPrices = [20.00, 50.00, 100.00, 10.00];

  // Call your function and print the new discounted list here
}
