/// Puzzle 13
///
/// Let's write a program to grade a student's test score!
/// 
/// Write a function called `getGrade` that takes an `int` (the score) 
/// and returns a `String` (the letter grade).
/// 
/// Use `if`, `else if`, and `else` to determine the grade based on these rules:
/// - 90 or above: Return "A"
/// - 80 to 89: Return "B"
/// - 70 to 79: Return "C"
/// - 60 to 69: Return "D"
/// - Below 60: Return "F"
///
/// Test it in `main()` by printing the results for a few different scores.
/// For example: `print(getGrade(85));` should print "B".

void main() {
  // Test your function here!
}
