/// Puzzle 7
///
/// You are building a simple music/movie app.
/// 1. Create a list of three of your favorite movies or
/// songs called `myFavorites`
/// 2. Write a function called `printPlaylist` that returns `void` and
/// takes ONE input: a list of Strings (Hint: the type is `List<String>`).
/// 3. Inside that function, use a `for-in` loop to loop through
/// the list and print `"Now playing: [item]"`.
/// 4. Call your function inside `main()` and pass your `myFavorites`
/// list into it.

void main() {}
