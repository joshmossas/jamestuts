/// Puzzle 4
///
/// Write a function named `formatPrice` that takes two inputs:
/// - a String called `itemName`
/// - a double called `itemPrice`
///
/// The function should return a single string that combines both of them.
/// For example, if the inputs are `"Apple"` and `0.99`, it should return
/// "Apple costs $0.99".
///
/// Test it: Call your function with `"Book"` and `14.99` in main and print
/// the result.
///
/// Hint: Because `$` a special character for string interpolation (ex: "Hello ${name}")
/// You need to put a `\` in front of it to display the actual "$" character:
/// - Example: "Thank you for purchasing ${name}. That will be \$${price}"

void main() {}
